# Scoped packages
@retrievalops:registry=https://registry.npmjs.org/

# Engine checks
engine-strict=true

# Auto-auth token (set via environment in CI)
# //registry.npmjs.org/:_authToken=${NPM_TOKEN}

# Signing (will use GPG key if available)
sign-git-commit=false
sign-git-tag=false
