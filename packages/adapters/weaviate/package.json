{
  "name": "@itsrajeshthota/retrievalops-weaviate",
  "version": "0.2.1",
  "description": "Weaviate adapter for RetrievalOps - GraphQL-based vector database with native FTS",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf dist"
  },
  "keywords": ["weaviate", "vector-database", "retrieval", "search", "graphql"],
  "author": "RetrievalOps Contributors",
  "license": "Apache-2.0",
  "dependencies": {
    "@itsrajeshthota/retrievalops-contracts": "*",
    "weaviate-ts-client": "^1.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^0.34.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

